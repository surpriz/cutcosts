# Application Configuration
APP_NAME=CutCosts
APP_ENV=development
DEBUG=true
SECRET_KEY=your-secret-key-change-in-production

# API Configuration
API_V1_PREFIX=/api/v1

# Security
ENCRYPTION_KEY=your-fernet-encryption-key-32-bytes-base64-encoded
JWT_SECRET_KEY=your-jwt-secret-key-change-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7
REFRESH_TOKEN_REMEMBER_ME_EXPIRE_DAYS=30

# Google OAuth
# Get these from Google Cloud Console > APIs & Services > Credentials > OAuth 2.0 Client ID
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:8000/api/v1/auth/google/callback

# Database
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/cloudwaste

# Redis
REDIS_URL=redis://localhost:6379/0

# Celery
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# CORS Configuration
# ⚠️  SECURITY: Comma-separated list of allowed origins (no wildcards!)
# - Development: Use http://localhost:PORT for local development
# - Production: Use HTTPS only (http:// only allowed for localhost/127.0.0.1)
# - Format: scheme://hostname[:port]
# - Examples:
#   * Development: http://localhost:3000,http://127.0.0.1:3000
#   * Production: https://cloudwaste.com,https://www.cloudwaste.com,https://app.cloudwaste.com
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# CORS credentials and caching
# - CORS_ALLOW_CREDENTIALS: Allow cookies and authorization headers (required for JWT)
# - CORS_MAX_AGE: Preflight cache duration in seconds (default: 600 = 10 minutes)
CORS_ALLOW_CREDENTIALS=true
CORS_MAX_AGE=600

# Email Configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASSWORD=your-smtp-password
EMAILS_FROM_EMAIL=noreply@cloudwaste.com
EMAILS_FROM_NAME=CutCosts

# Email Verification
EMAIL_VERIFICATION_TOKEN_EXPIRE_HOURS=168
UNVERIFIED_ACCOUNT_CLEANUP_DAYS=14
FRONTEND_URL=http://localhost:3000

# AWS Configuration (Optional - for testing)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=eu-west-1

# AWS SES Configuration (Optional - for cold email monitoring)
AWS_SES_REGION=eu-north-1
AWS_SES_ACCESS_KEY_ID=
AWS_SES_SECRET_ACCESS_KEY=

# AI Assistant (Anthropic)
ANTHROPIC_API_KEY=
CHAT_MAX_MESSAGES_PER_USER_PER_DAY=50
CHAT_CONTEXT_MAX_RESOURCES=20
CHAT_MODEL=claude-haiku-4-5-20250818

# Rate Limiting Configuration
# Enable or disable rate limiting globally
RATE_LIMIT_ENABLED=true

# Authentication endpoints (most critical - prevent brute-force)
RATE_LIMIT_AUTH_LOGIN=5/minute
RATE_LIMIT_AUTH_REGISTER=3/minute
RATE_LIMIT_AUTH_REFRESH=10/minute

# Resource-intensive operations
RATE_LIMIT_SCANS=10/minute

# Admin endpoints
RATE_LIMIT_ADMIN=50/minute

# Default limit for all API endpoints
RATE_LIMIT_API_DEFAULT=100/minute

# Stripe Payment Configuration
# Get these keys from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your_secret_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here
# Get webhook secret from: https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here
# Price IDs from Stripe Dashboard (after creating products)
STRIPE_PRICE_ID_PRO=price_xxxxxxxxxxxxx
STRIPE_PRICE_ID_ENTERPRISE=price_xxxxxxxxxxxxx
